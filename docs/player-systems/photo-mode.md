# Photo Mode

**Version:** 1.0.0\
**Module:** `PhotoMode`

Pause the game, move a free camera, apply filters, pose your Pokémon, and export screenshots as PNG.

<!-- TODO: Add control scheme, filter list, example photos -->

---

## Features

- Pause + free camera movement
- Visual filters (sepia, noir, vivid, etc.)
- Pokémon posing
- PNG export to game folder

---

## Configuration

See `000_Config.rb` — `ENABLED = true` to activate.
